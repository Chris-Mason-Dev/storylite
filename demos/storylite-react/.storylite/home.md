---
title: React Demo
description: React stories rendered through the StoryLite renderer adapter.
---

# React Demo

This project keeps StoryLite's normalized story model unchanged while rendering React components as
another adapter.

## Coverage

- React buttons, cards, fields, and composed layout stories
- Args mapped to React props
- Cleanup when switching between renderer types
